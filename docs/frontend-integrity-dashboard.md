# Frontend Integrity Dashboard

Adds a reusable dashboard component for:
- parser health score
- repair count
- style preload count
- PCM map count
- runtime readiness

This does not replace existing UI.
It is safe to wire into the AI/dashboard page later.
