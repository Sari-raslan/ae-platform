# Runtime Live Store

Adds stateful live runtime storage:

- update runtime analysis
- get runtime snapshot
- execute runtime commands
- connect runtime response to realtime arranger engine

This prepares API command endpoints and live dashboard refresh.
