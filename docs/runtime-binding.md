# Runtime Binding

Connects parsed analysis data into the live arranger runtime.

Adds:
- parsed STYLE catalog to style preload queue
- PCM diagnostics to PCM preload cache
- SongBook links to runtime map
- runtime API response builder

This is the bridge from static analysis to live runtime execution.
